name: Check shell scripts

on:
  push:
    branches:
      - develop
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Run ShellCheck
      uses: ludeeus/action-shellcheck@master
      with:
        scandir: './ansible/files/admin_scripts'